[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: uses temp sqlite/filesystem persistence (still offline, no network)
filterwarnings =
    ignore::DeprecationWarning
