/**
 * 反馈类组件统一出口
 * - ErrorBoundary: 错误边界
 * - ErrorFallback: 默认错误兜底 UI
 * - PageSkeleton: 路由懒加载骨架屏
 */
export { ErrorBoundary } from './ErrorBoundary';
export { ErrorFallback } from './ErrorFallback';
export { PageSkeleton } from './PageSkeleton';
